#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../flatiron/bin/flatiron" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../flatiron/bin/flatiron" "$@"
  ret=$?
fi
exit $ret
