#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../http-proxy/bin/node-http-proxy" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../http-proxy/bin/node-http-proxy" "$@"
  ret=$?
fi
exit $ret
